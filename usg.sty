\ProvidesPackage{usg-agenda}

\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{enumerate, enumitem}
\setenumerate{nosep}
\RequirePackage{ifthen}
\RequirePackage{etoolbox}
\usepackage{kvoptions}
  \SetupKeyvalOptions{%
	  family=usg,
	  prefix=usg@,
  }
\DeclareStringOption[AAC]{unit}[AAC]
\DeclareStringOption[Agenda]{documenttype}[Agenda]
\DeclareStringOption[Academic Affairs Committee]{longUnit}[Academic Affairs Committee]
\DeclareStringOption[../../package]{root}[../../package]
\DeclareStringOption[\usg@unit\ \usg@documenttype \\\today]{title}[\usg@unit \usg@documenttype \\\today]
\ProcessKeyvalOptions*

\renewcommand\labelenumi{\arabic{enumi})}
\renewcommand\labelenumii{\Alph{enumii})}
\renewcommand\labelenumiii{(\roman{enumiii})}

\usepackage[nodayofweek]{datetime}
\usepackage[short, american]{isodate}

%In case the current date doesn't match the date of the meeting, comment in the following lines with the correct dates
%\year=2018
%\month=3
%\day=13

\def\shortyear#1{\expandafter\shortyearhelper#1}
\def\shortyearhelper#1#2#3#4{#3#4}

\usepackage{amsmath,amssymb,amsthm}
\usepackage{marvosym}
\usepackage{graphicx}
\usepackage[dvipsnames]{color}
\definecolor{Blue}{rgb}{0.0, 0.02, 0.15}
\usepackage{titling}
\setlength{\droptitle}{20pt}

%opening
\title{\textbf{\usg@title}}
\author{\vspace{-5ex}}
\date{\vspace{-5ex}}
\usepackage[headheight=10pt, includefoot]{geometry}
%\AtBeginDocument{\vspace*{2\baselineskip}}

\usepackage{eurosym}
\DeclareUnicodeCharacter{20AC}{\euro}

\usepackage{fancyhdr}
\fancypagestyle{main}{
	\lhead{\vspace*{-20\baselineskip}\ifcase \thepage \or\includegraphics[width=4cm]{\usg@root/jacobs-logo}\else \usg@title \fi}
	\chead{\ifcase \thepage \or \textcolor{Blue}{\usg@longUnit\\Undergraduate Student Government\\Jacobs University Bremen}\else \fi}
	\rhead{\ifcase \thepage \or \includegraphics[width=2.5cm]{\usg@root/usg-logo.jpg}\else \usg@longUnit \fi}
	\lfoot{}
    % shouldn't be longer than a page	
	%\cfoot{\ifcase \thepage \or \else Page \thepage{} of \pageref{theEnd} \fi}
   	\cfoot{\ifcase \thepage \or \else Page \thepage{} of \pageref{theEnd} \fi}	
	%\rfoot{\today, 8.45pm, Nordmetall Conference Room}
	\renewcommand{\headrulewidth}{0pt}
}
\pagestyle{main}
\AtBeginDocument{
	\maketitle
	\thispagestyle{main}
}

\usepackage{hyperref}

\hypersetup{pdftitle="\text{\usg@unit\_\usg@documenttype\_\twodigit{\shortyear{\the\year}}\twodigit{\the\month}\twodigit{\the\day}}", hidelinks}

%\renewcommand\thesection{\arabic{section})}
%\renewcommand{\section}{\paragraph}